# Правила окон
# Новый синтаксис Hyprland 0.48+

# Блокировать максимизацию для всех окон
windowrule = match:class ^(.*)$, suppress_event maximize

# Установить прозрачность: 0.97 для активных, 0.9 для неактивных окон
windowrule = match:class ^(.*)$, opacity 0.97 0.9
