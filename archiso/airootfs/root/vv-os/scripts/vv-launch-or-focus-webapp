#!/bin/zsh

# Запуск webapp или фокусировка на существующем окне
# Адаптировано из Omarchy для VV Shell

if (($# == 0)); then
  echo "Usage: vv-launch-or-focus-webapp [window-pattern] [url-and-flags...]"
  exit 1
fi

WINDOW_PATTERN="$1"
shift
LAUNCH_COMMAND="vv-launch-webapp $@"

exec vv-launch-or-focus "$WINDOW_PATTERN" "$LAUNCH_COMMAND"
